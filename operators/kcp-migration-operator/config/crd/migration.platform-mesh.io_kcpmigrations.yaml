---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: kcpmigrations.migration.platform-mesh.io
spec:
  group: migration.platform-mesh.io
  names:
    kind: KCPMigration
    listKind: KCPMigrationList
    plural: kcpmigrations
    shortNames:
    - kcpmig
    singular: kcpmigration
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.source.kind
      name: Source Kind
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.statistics.resourcesSynced
      name: Synced
      type: integer
    - jsonPath: .status.statistics.resourcesFailed
      name: Failed
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KCPMigration is the Schema for the kcpmigrations API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KCPMigrationSpec defines the desired state of KCPMigration
            properties:
              source:
                description: Source defines the source resources to watch
                properties:
                  apiVersion:
                    description: APIVersion of the source resource (e.g., apps.example.com/v1)
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*\/v[a-z0-9]+(alpha[0-9]+|beta[0-9]+)?$
                    type: string
                  kind:
                    description: Kind of the source resource (e.g., MyApp, TenantConfig)
                    minLength: 1
                    type: string
                  labelSelectors:
                    description: |-
                      LabelSelectors is a list of label selectors to filter source resources.
                      Resources must match ALL selectors (AND logic).
                      Supports standard Kubernetes label selector syntax:
                      - Equality: "app=myapp", "env!=dev"
                      - Set-based: "env in (prod,staging)", "tier notin (frontend)"
                      - Existence: "app" (key exists), "!temp" (key doesn't exist)
                    items:
                      type: string
                    type: array
                  namespace:
                    description: Namespace to filter source resources (optional, empty
                      = all namespaces)
                    type: string
                required:
                - apiVersion
                - kind
                type: object
              syncOptions:
                description: SyncOptions provides optional configuration for sync
                  behavior
                properties:
                  rateLimit:
                    description: RateLimit controls how fast resources are synced
                    properties:
                      burst:
                        default: 100
                        description: Burst is the maximum burst size for the rate
                          limiter
                        maximum: 2000
                        minimum: 1
                        type: integer
                      resourcesPerSecond:
                        default: 50
                        description: ResourcesPerSecond is the maximum resources to
                          sync per second
                        maximum: 1000
                        minimum: 1
                        type: integer
                    type: object
                type: object
              transform:
                description: Transform defines how source resources are transformed
                  and where they are placed in kcp
                properties:
                  targetNamespace:
                    description: |-
                      TargetNamespace specifies the namespace in the target workspace
                      Supports Go template syntax. If not specified, uses source namespace or cluster-scoped
                    type: string
                  targetWorkspace:
                    description: TargetWorkspace defines how to derive the target
                      kcp workspace
                    properties:
                      expression:
                        description: |-
                          Expression is a Go template expression to derive the workspace path
                          Has access to .Source (full resource), .Source.metadata.namespace, .Source.metadata.name, etc.
                        minLength: 1
                        type: string
                    required:
                    - expression
                    type: object
                  template:
                    description: |-
                      Template defines the target resource structure
                      If not specified, resource is synced as-is (pass-through mode)
                    properties:
                      configMapRef:
                        description: ConfigMapRef references a ConfigMap containing
                          the template
                        properties:
                          key:
                            default: template.yaml
                            description: Key in the ConfigMap. Defaults to "template.yaml"
                            type: string
                          name:
                            description: Name of the ConfigMap
                            type: string
                        required:
                        - name
                        type: object
                      inline:
                        description: Inline template for target resource as raw JSON/YAML
                        x-kubernetes-preserve-unknown-fields: true
                    type: object
                required:
                - targetWorkspace
                type: object
            required:
            - source
            - transform
            type: object
          status:
            description: KCPMigrationStatus defines the observed state of KCPMigration
            properties:
              childOperator:
                description: ChildOperator contains information about the child operator
                  deployment
                properties:
                  name:
                    description: Name of the child operator Deployment
                    type: string
                  namespace:
                    description: Namespace of the child operator
                    type: string
                  ready:
                    description: Ready indicates whether the child operator is ready
                    type: boolean
                type: object
              conditions:
                description: Conditions represent the latest available observations
                  of the migration's state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              nextReconcileTime:
                description: NextReconcileTime is the next scheduled reconciliation
                  time
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the last observed generation of
                  the KCPMigration
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the migration
                type: string
              statistics:
                description: Statistics contains sync statistics
                properties:
                  lastSyncTime:
                    description: LastSyncTime is the last successful sync time
                    format: date-time
                    type: string
                  resourcesFailed:
                    description: ResourcesFailed is the number of resources that failed
                      to sync
                    format: int64
                    type: integer
                  resourcesSynced:
                    description: ResourcesSynced is the total number of resources
                      successfully synced
                    format: int64
                    type: integer
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
