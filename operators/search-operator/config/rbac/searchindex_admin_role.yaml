# This rule is not used by the project search-operator itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants full permissions ('*') over core.platform-mesh.io.
# This role is intended for users authorized to modify roles and bindings within the cluster,
# enabling them to delegate specific permissions to other users or groups as needed.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: search-operator
    app.kubernetes.io/managed-by: kustomize
  name: searchindex-admin-role
rules:
- apiGroups:
  - core.platform-mesh.io
  resources:
  - searchindexes
  verbs:
  - '*'
- apiGroups:
  - core.platform-mesh.io
  resources:
  - searchindexes/status
  verbs:
  - get
