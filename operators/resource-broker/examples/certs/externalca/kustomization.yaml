resources:
  # Only the AcceptAPI CRD is needed but mcmanager requires all CRDs at
  # the moment.
  - ../../../config/crd/bases/broker.platform-mesh.io_acceptapis.yaml
  - ../../../config/crd/bases/broker.platform-mesh.io_migrationconfigurations.yaml
  - ../../../config/crd/bases/broker.platform-mesh.io_migrations.yaml

  # The issuer to issue certificates
  - issuer.yaml
  # The resource graph to project the generic api to cert-manager
  - rgd.yaml
  # The AcceptAPI for resource broker
  - acceptapi.yaml
